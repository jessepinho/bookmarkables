import * as React from 'react';
import { Registry } from './schema';

interface Props {
  registry: Registry;
}

const Search = ({ registry }: Props) => {
  return <div>search</div>;
};

export default Search;
